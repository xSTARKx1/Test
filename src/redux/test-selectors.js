const getImages = state => state.api.images;
const getIsLoading = state => state.api.isLoading;
const getUserAge = state => state.api.userAge;
const getUserData = state => state.api.userData;

export default { getImages, getIsLoading, getUserAge, getUserData };
